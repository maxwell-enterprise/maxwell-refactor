/**
 * Compatibility re-export. SeedService has moved to shared/.
 * @deprecated Prefer importing from './shared' or '@/services/shared' for new code.
 */
export { SeedService } from './shared/seedService';
