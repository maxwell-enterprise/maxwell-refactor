/**
 * Compatibility re-export. ConfigService has moved to shared/.
 * @deprecated Prefer importing from './shared' or '@/services/shared' for new code.
 */
export { ConfigService } from './shared/configService';
