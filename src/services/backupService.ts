/**
 * Compatibility re-export. BackupService has moved to shared/.
 * @deprecated Prefer importing from './shared' or '@/services/shared' for new code.
 */
export { BackupService, type BackupData } from './shared/backupService';
