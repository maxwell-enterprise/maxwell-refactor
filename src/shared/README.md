# shared

Shared UI, hooks, and utilities used across multiple features. Intended for gradual migration from `src/components` (e.g. atoms, molecules, common). Do not move files here until explicitly migrating.
