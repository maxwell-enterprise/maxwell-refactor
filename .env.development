# `next dev` — Nest API (global prefix `/fe`). Paths are relative to this base (e.g. `/store/inventory`).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3002/fe
# Hit Railway while on dev: NEXT_PUBLIC_USE_PRODUCTION_API=true and set API URL above to Railway `/fe`.
